# Directory / Admin Entry Points

This folder is reserved for secondary entry points.

- `DirectoryAdmin.tsx`: Dedicated admin directory entry point.

Note: This repo currently does not use react-router routes for pages; the admin UI is still available via `App.tsx` as an overlay. The `DirectoryAdmin` page is created as a standalone entry so you can later wire it to a route (e.g. `/admin`).

